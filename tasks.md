# Tasks — MealSense (Full Project)

**Status:** Not started (Phase 0 partially applicable — see notes)
**Date:** 2026-08-21
**Requirements:** [requirements.md](requirements.md)

Phases are ordered by risk and dependency, not by how interesting they are. Security comes first because health data and order records are currently unprotected by anything in this repo. Each phase should land as its own PR — that keeps every resume/interview claim about this project traceable to real, reviewable work.

---

## Phase 1 — Security Hardening (do this first) ✅ done (2026-09-07)

Traces to requirements §8. This is the biggest real gap in the project — not a stretch goal.

- [x] 1.1 Write `firestore.rules` and commit it to the repo — `mealsense-app/firestore.rules`
- [x] 1.2 Rule: a student can only read/write their own `users/{uid}` document
- [x] 1.3 Rule: a student can only read their own `orders` (query by `studentId`), never another student's
- [x] 1.4 Rule: only accounts with `role: "kitchen"` can transition `orders.status` toward `ready`/`completed`; the owning student can independently advance `placed → preparing → ready` (matches the existing client-side demo timer in `OrderStatusScreen.tsx` — this is enforced by the same rule that also guarantees `items`/`totalPrice`/`studentId` are immutable on every status-advancing write
- [x] 1.5 Audited how `role: "kitchen"` actually got assigned: **`RegisterScreen.tsx` let any user self-select it at signup with zero gatekeeping** — a real privilege-escalation bug, not hypothetical. Fixed by (a) rules that reject any client-created user doc with `role != "student"`, (b) removing the role picker from `RegisterScreen.tsx` entirely, (c) documenting the real provisioning path in `mealsense-app/README-kitchen-accounts.md` (manual console edit for now; a real admin flow is Phase 4)
- [x] 1.6 Tested rules with the Firebase emulator (`@firebase/rules-unit-testing` + Node's built-in test runner, no Jest needed) — `mealsense-app/tests/firestore.rules.test.js`, 16/16 passing via `npm run test:rules`
- [x] 1.7 Added a profile + associated data deletion flow — `ProfileScreen.tsx`'s "Delete My Account & Data" batch-deletes the student's orders, deletes their `users/{uid}` doc, then deletes the Auth account itself
- [x] 1.8 Deployed rules to the live project — a fresh Firebase project (`mealsense-cb5ab`) was created to replace the old auto-named `myproject-dc745` (whose live rules were undocumented), `.firebaserc`, `src/config/firebase.ts`, and `.env` were repointed at it, Firestore (Standard edition) and Email/Password Auth were enabled, and `npx firebase deploy --only firestore:rules` confirmed released. `firestore.rules` now actually governs production, not just the emulator.

## Phase 2 — Testing Infrastructure ✅ done (2026-09-05)

Traces to requirements §9. Scoped tightly to the recommendation engine's real safety invariant (zero-false-negative allergen filtering) plus API route coverage.

**Result:** 47/47 tests passing, `services/recommendation_engine.py` at 100% line coverage (target was ≥90%). Files: `mealsense-api/tests/conftest.py`, `test_recommendation_engine.py`, `test_routes.py`, `pytest.ini`, `requirements-dev.txt`. Run with `cd mealsense-api && venv/bin/python -m pytest --cov=services --cov-report=term-missing`. This unblocks Phase 6 (the bandit layer), which was explicitly gated on this phase landing first.

### 2.1 Test Infrastructure Setup
- [x] 2.1.1 Add `pytest` and `httpx` to `mealsense-api/requirements.txt` (or a new `requirements-dev.txt`)
- [x] 2.1.2 Create `mealsense-api/tests/` package with `__init__.py`
- [x] 2.1.3 Add `pytest.ini` (or `pyproject.toml` `[tool.pytest.ini_options]`) setting `testpaths = tests`
- [x] 2.1.4 Verify `pytest` runs from `mealsense-api/` with `python -m pytest`
- [x] 2.1.5 Add `conftest.py` with shared fixtures: a minimal valid student profile, a small fixture menu (allergen item, vegan item, high-protein item, off-period item)

### 2.2 Recommendation Engine Unit Tests (`test_recommendation_engine.py`)
- [x] 2.2.1 Allergen hard filter: single flagged allergen excludes matching item
- [x] 2.2.2 Allergen hard filter: case-insensitive matching
- [x] 2.2.3 Allergen hard filter: multiple simultaneous allergies all enforced
- [x] 2.2.4 Dietary identity filter: single identity (e.g. vegetarian) enforced
- [x] 2.2.5 Dietary identity filter: multiple identities enforced as AND, not OR
- [x] 2.2.6 Meal-period availability filter enforced correctly
- [x] 2.2.7 No-safe-items fallback returns `null` + `"no_safe_items"`
- [x] 2.2.8 Macro/calorie alignment score decreases monotonically as calories diverge from target
- [x] 2.2.9 Protein/fiber scores capped at reference max (50g / 15g)
- [x] 2.2.10 Variety penalty applied for items in `recent_ids`
- [x] 2.2.11 Final item score bounded to [0, 100]
- [x] 2.2.12 Diabetes profile shifts weight toward `sugar_sodium`
- [x] 2.2.13 Hypertension (without diabetes) applies the same shift
- [x] 2.2.14 Diabetes + hypertension together: no double-adjustment
- [x] 2.2.15 Adjusted weights sum to 1.0, parametrized over all condition combinations
- [x] 2.2.16 BMR fixed test vectors for male and female profiles
- [x] 2.2.17 All four activity multipliers applied correctly
- [x] 2.2.18 Goal adjustments (lose/gain/maintain) applied correctly
- [x] 2.2.19 Meal-period apportionment matches README §8.4 table
- [x] 2.2.20 Top recommendation is always the highest-scoring candidate
- [x] 2.2.21 Alternatives capped at 3, sorted descending
- [x] 2.2.22 Reasoning signals capped at 4

### 2.3 API Route Tests (`test_routes.py`)
- [x] 2.3.1 `GET /menu` with no params returns all items + correct `count`
- [x] 2.3.2 `GET /menu?meal_period=X` filters correctly, including `all_day` items
- [x] 2.3.3 `POST /recommendation` with a minimal valid profile returns 200 + expected shape
- [x] 2.3.4 `POST /recommendation` with malformed/missing fields returns a 4xx (document actual behavior)

### 2.4 Coverage
- [x] 2.4.1 Add `pytest-cov`; run `pytest --cov=services --cov-report=term-missing`
- [x] 2.4.2 Confirm `services/recommendation_engine.py` hits ≥90% line coverage

**Definition of done for the resume claim:** 2.1–2.3 complete. At that point "wrote a pytest suite covering the recommendation engine's hard-filter and scoring logic" is accurate and defensible in an interview.

## Phase 3 — Menu Data Pipeline

Traces to requirements §3. Currently the entire menu is one hardcoded Python file; nothing here is built yet despite being a documented architecture decision.

- [x] 3.1 **Menu persistence layer** ✅ done (2026-09-08, all four subtasks below complete) — Firestore, via `google.cloud.firestore.Client` added to `mealsense-api` for the first time (decided 2026-09-08, see requirements.md). Layout: `menus/{servedOn}/items/{itemId}` subcollection per day, `servedOn` supplied at upload time (task 3.2), not parsed per-item by the adapters — keeps `menu_ingestion/` untouched. `sample_menu.py` stays as a fallback when Firestore has nothing for the requested date, not replaced outright.
  - [x] 3.1.1 ✅ done (2026-09-08) — Added `firebase-admin==7.5.0` to `requirements.txt`. Actual Firestore access goes through `google.cloud.firestore.Client` directly (`services/firestore_client.py`), not `firebase_admin.firestore.client()` — confirmed hands-on that the latter's credential loading calls `google.auth.default()` even with `FIRESTORE_EMULATOR_HOST` set, which raises with no GCP environment configured. Anonymous credentials against the emulator, a real service account file (`GOOGLE_APPLICATION_CREDENTIALS`) in production; raises `FirestoreNotConfiguredError` if neither is set, which `menu_store.py` catches specifically to fall back to the sample menu (a real connection/permissions error still propagates, not masked). **Manual step still pending, not blocking:** a real service account key from the Firebase console, not needed until we go past emulator-only testing.
  - [x] 3.1.2 ✅ done (2026-09-08) — `services/menu_store.py`: `get_menu_for_date(served_on)` and `write_menu_items(served_on, items)` (batch write, keyed by each item's stable id so re-uploading a day overwrites matching dishes instead of duplicating). Tested against a real local Firestore emulator, not mocked (`tests/test_menu_store.py`), same reasoning as the JS rules tests — skips cleanly with a clear reason if the emulator isn't running rather than a raw connection error.
  - [x] 3.1.3 ✅ done (2026-09-08) — `routers/menu.py` and `routers/recommendations.py` now read via `menu_store.get_menu_for_date` instead of importing `SAMPLE_MENU` directly; both accept an optional `served_on` override, defaulting to today. Existing `test_routes.py` still passes unmodified with no emulator running, exercising the sample-menu fallback path.
  - [x] 3.1.4 ✅ N/A, resolved by architecture (2026-09-08) — originally scoped as new Firestore security rules for `menus/{date}/items/{itemId}`, but rules only govern Firebase Auth-authenticated client SDK access, and the mobile app never touches this collection directly — it reads menu data exclusively through the FastAPI `/menu` and `/recommendation` endpoints, which access Firestore server-side via a service account (or the emulator equivalent), bypassing rules entirely by design. The existing catch-all deny-all in `firestore.rules` already correctly blocks any accidental direct client access. Unlike `users`/`orders`, this collection needs no explicit rule unless the mobile app ever starts reading menu data directly via Firestore instead of through the API.
- [x] 3.2 **Admin CSV/JSON upload endpoint** ✅ done (2026-09-08) — `POST /admin/menu/upload?format=csv|messy_json&served_on=YYYY-MM-DD` (`routers/admin_menu.py`), runs the request body through the matching `menu_ingestion` adapter, writes accepted items via `menu_store.write_menu_items`, returns `{accepted, rejected}` (each rejected entry carries the raw record and reason) matching README §7.2. Gated by `services/auth.py::require_kitchen_role` — verifies a Firebase ID token, then checks `role == "kitchen"` in Firestore (no separate `admin` role exists yet; reusing `kitchen` is a deliberate scope decision, revisit once Phase 4 formalizes real admin accounts). Tested against the real Firestore emulator: upload/accept/reject behavior for both formats, persisted items actually readable back via `get_menu_for_date`, and — deliberately without overriding the auth dependency — a request with no Authorization header genuinely gets rejected by the live route, not just by an isolated unit test of the auth function. Found and fixed one real bug along the way: a required `Header(...)` parameter meant a *missing* header returned FastAPI's generic 422 instead of the app's own 401, inconsistent with a malformed header; changed to an optional header handled explicitly. 116/116 tests passing (98% coverage across `services/` and `routers/`; the only gaps are the real-service-account and real-token-verification production paths, which need actual Firebase infrastructure to test meaningfully and aren't worth faking).
- [x] 3.1a **Generic ingestion & normalization adapter** (design-spec.md §7.0) ✅ done (2026-09-08) — `mealsense-api/services/menu_ingestion/`: `schema.py` (canonical `MenuItem` builder + validation, matching the flat shape the engine actually reads, not README §7.1's nested sketch), `base.py` (the `MenuAdapter` protocol + `IngestionResult`/`RejectedRecord`), `csv_adapter.py` (README §7.2's column spec), `messy_json_adapter.py` (arbitrary key aliases, units embedded in numeric strings, meal-period free text). Enforces the core safety invariant end-to-end: a record with allergen data entirely absent from the source is rejected, never silently treated as zero allergens (distinct from an explicit empty list, which is accepted) — this was previously only a documented intent (design-spec.md §2.3), not actually enforced anywhere in code. 97/97 tests passing, 99% coverage (`tests/test_menu_ingestion.py`). Stable per-dish IDs (hash of name+station, not date-dependent) verified identical across both adapters for the same logical dish.
- [x] 3.3 **Manual entry UI for dining staff** ✅ done (2026-09-11) — `mealsense-app/src/screens/kitchen/AddMenuItemScreen.tsx`, reachable via a "+ Menu Item" button on `KitchenDashboardScreen.tsx` (kitchen-role accounts only, matching 3.2's reused-role decision — no separate admin role yet). A form for all `MenuItem` fields (name, station, meal period, optional time window, all seven nutrition fields, price, allergens, dietary tags, ingredients, description) that submits a single-item payload to the existing `POST /admin/menu/upload?format=messy_json` endpoint from 3.2 — no new backend route needed, just a client of the one that already existed. Allergens use an explicit three-state control (undecided / "None, verified" / one-or-more selected); undecided blocks submission client-side, mirroring the ingestion layer's own fail-closed rule so staff can't accidentally submit an item with unknown allergen status. Found and fixed a real gap this surfaced: `messy_json_adapter.py` had `available_from`/`available_until` hardcoded to `None` always (never read from the source record at all) and never extracted `ingredients`/`description` either — all four are now wired through via key aliases, with 5 new adapter tests. 134/134 backend tests passing, `npx tsc --noEmit` clean. This is the entry point Phase 4's admin dashboard will build on, not the dashboard itself — no sold-out toggle or aggregate analytics view yet (still Phase 4 scope).
- [x] 3.4 **Real vendor integration — Santa Clara University** ✅ done (2026-09-11), closes README §12 Open Question #1 for a first campus — `services/vendor_ingestion/` (`campus_config.py`, `bon_appetit.py`), wired into a new `POST /admin/menu/sync-vendor?campus=<id>` route (`routers/admin_menu.py`, same kitchen-role gating as 3.2).
  - **Vendor confirmed:** SCU dining is run by Bon Appétit Management Co. (`scudining.cafebonappetit.com`), not Nutrislice/Cbord as README §7.2 guessed — confirmed against the live site, not assumed.
  - **The documented JSON API is a dead end without a vendor relationship:** `legacy.cafebonappetit.com/api/2/...` now returns "Cafemanager no longer accepts unauthenticated requests" — needs vendor-issued credentials, same category of blocker as the original open question. Instead, each café's public page embeds a real structured data blob (`Bamco.dayparts` / `Bamco.menu_items`) with everything needed (name, station, per-daypart time window, full nutrition, price, ingredient text, allergen/dietary icons) — reachable with no auth, verified live 2026-09-11 (36 items correctly parsed off Mission Bakery Cafe's real page). Documented in `bon_appetit.py`'s module docstring as *not* a stable public contract — it can break if Bon Appétit changes their theme.
  - **Allergen trust policy — a real safety decision, not an implementation detail:** Bon Appétit's own SCU allergen-disclosure page says their online data isn't a complete substitute for asking kitchen staff directly, and every location handles all top-9 allergens in shared prep areas. Presented this as a choice to the user rather than deciding silently; chosen answer: an item's allergen icons are trusted as a positive-only signal (an icon present is real), but **zero icons means unknown, not verified-safe** — `allergens=None`, rejected by `build_menu_item`'s existing fail-closed rule, same as everywhere else in this codebase. On the live smoke test this correctly held out `Hot Coffee`/`Hot Tea`/`Espresso` (genuinely untagged) while accepting 36 items with real allergen data (e.g. `Hot Latte` → `['dairy']`, tag `['vegetarian']`).
  - **General template, not a one-off:** onboarding another Bon Appétit campus is one new `CampusConfig` entry in `campus_config.py` (base URL + café slugs) — nothing else changes. A campus on a different vendor needs one new adapter module implementing the same `fetch_campus_menu(campus) -> IngestionResult` shape, registered by vendor key in `admin_menu.py`'s `_VENDOR_FETCHERS` map — the route, the review-queue wiring, and `menu_store` persistence are all vendor-agnostic already.
  - **Also built the review-queue half of 3.6** (design-spec.md §7.0a case 3) as plumbing this needed anyway: `services/menu_review_queue.py`, a Firestore-backed queue that allergen-rejected records land in instead of being silently dropped. No admin UI reads it yet (Phase 4) — `get_review_queue`/`resolve_review_item` exist so it's a real, testable read path, not a write-only sink.
  - **Deliberately not built:** a nightly scheduled sync (README §7.1's "02:00 local time" job) — there's no cron/scheduler infrastructure in this project at all (Phase 7). `sync-vendor` is manually triggered, matching the existing manual-upload pattern from 3.2.
  - 44 new tests (`test_bon_appetit_adapter.py` — pure parsing logic, no network; `test_menu_review_queue.py` — real Firestore emulator; 4 new route tests in `test_admin_menu.py`), 172/172 passing, 98% coverage across `services/`+`routers/` (the only real gaps are the live-network fetch call itself and two malformed-JSON defensive branches — deliberately not faked, same stance as the existing real-service-account/real-token-verification gaps).
- [x] 3.5 **Real time-window availability** ✅ done (2026-09-11) — `_passes_hard_filters` (`recommendation_engine.py`) now takes an optional `now: time` and rejects items outside `[available_from, available_until]`; items with no explicit window default to `DEFAULT_MEAL_WINDOWS` per README §7.3 (breakfast 07:00–10:30, lunch 11:00–15:00, dinner 17:00–21:00, matching the sample menu's own values). `recommend()` defaults `now` to the real wall clock when not passed, so every production call is actually time-filtered now, not just meal-period-bucketed. `now=None` (the default for direct `_passes_hard_filters` calls) skips the check entirely rather than defaulting to the real clock — deliberate, so every pre-existing test that only cares about allergen/dietary/meal-period-bucket filtering stays deterministic regardless of wall-clock time when the suite runs; only `recommend()`-level tests and the new availability tests pass an explicit `now`. 8 new tests in `test_recommendation_engine.py`, engine still at 100% coverage, 131/131 passing overall.
- [x] 3.6 **Incomplete data handling** ✅ done (2026-09-11, design-spec.md §7.0a) — all three paths built, scoped to the vendor-sync ingestion path only (3.4), not the CSV/messy-JSON manual uploads.
  - **(a) Missing nutrition fields** — `services/menu_ingestion/llm_enrichment.py::estimate_missing_nutrition`, called from `bon_appetit.py` for exactly the numeric fields an item is missing (never a field already supplied — the core cost/scope constraint). Accepted outright (low-stakes if wrong per the design doc), stored on the item as `estimated_fields: [...]`. Price is explicitly excluded from this fallback — wrong nutrition data degrades a recommendation, a wrong price is a money-safety issue, so price stays fail-closed exactly like 3.6a already decided.
  - **(b) Missing structured allergens, ingredient text present** — `llm_enrichment.py::extract_allergens_from_ingredients` returns an allergen list plus a confidence score; `bon_appetit.py` only accepts it at ≥ 0.9 confidence (`ALLERGEN_CONFIDENCE_THRESHOLD`), otherwise the item stays excluded exactly as before. Decided 2026-09-11 (a real safety call, not left to default behavior): even a confident, accepted extraction **still writes a review-queue entry** ("AI-extracted allergens, please verify") — an LLM clearing the bar never gets the same zero-further-visibility treatment as a real vendor icon or a human confirmation.
  - **(c) Missing allergens, no ingredient text at all** — the review queue itself (built as part of 3.4, see that entry) — stays excluded, queued, no model involved, unchanged from that design.
  - **Model:** `claude-opus-5` via `client.messages.parse(..., output_format=<pydantic model>)` for structured output. Every call fails soft (returns `{}`/`None` on any error, never raises into the ingestion pipeline) — a Claude outage degrades to "reject like before," not a crash.
  - The LLM's role stays strictly limited to producing cleaner *input* for the adapter layer, per the design doc's closing line — `recommendation_engine.py` was not touched, and has no branch that even looks at `estimated_fields`.
  - 19 new tests (11 in `test_llm_enrichment.py` — pure logic, injectable fake client, no network/API key needed; 8 more in `test_bon_appetit_adapter.py` for the wiring itself), 191/191 passing, 98% coverage across `services/`+`routers/`.
  - **Live-verified (2026-09-11):** a real `ANTHROPIC_API_KEY` was provisioned (`mealsense-api/.env`, gitignored, loaded via `python-dotenv` in `main.py`) and both functions were exercised against the real API, not just the test suite's fake client — `estimate_missing_nutrition` correctly filled only the two fields asked for (`fiber_g`, `sugar_g`) and left everything else untouched; `extract_allergens_from_ingredients` correctly identified `gluten`/`sesame`/`soy` from a real ingredient list but reported `confidence: 0.55` — below the 0.9 bar, confirming the threshold gate actually holds back a real low-confidence result rather than only a synthetic test one.
- [x] 3.6a **Missing `price` field** ✅ done (2026-09-11) — resolved the open product decision by requiring `price` at ingestion, rejecting like allergens/nutrition rather than blocking-at-order-time: `schema.py::build_menu_item` now validates `price` through the same numeric-required path as the seven nutrition fields, so a record with no usable price never reaches the store. `csv_adapter.py` reads a `price` column (added to the documented spec, design-spec.md §7.2); `messy_json_adapter.py` accepts `price`/`Price`/`cost`/`Cost` aliases. `HomeScreen.tsx`'s "Price unavailable" fallback (2026-09-10) stays in place as defense-in-depth but should no longer trigger for anything ingested through the fixed pipeline. 6 new tests, 123/123 passing, 98% coverage.
- [x] 3.7 **Fix fail-open allergen default in the engine itself** ✅ done (2026-09-08) — `_passes_hard_filters` now checks `item.get("allergens")` for `None` (missing key) and fails closed unconditionally, before even comparing against the student's own allergy list, distinct from an explicit empty list which still passes. Two new tests (`test_allergen_filter_fails_closed_on_missing_allergen_data`, `test_allergen_filter_accepts_explicit_empty_allergen_list`); full suite still at 99/99 passing, engine still 100% coverage.

## Phase 4 — Admin Dashboard (Dining Staff) ✅ done (2026-09-14)

Traces to requirements §6. Originally scoped as "distinct from the Kitchen Dashboard, which only handles order fulfillment" — in practice, every admin capability landed inside the Kitchen Dashboard's own navigator instead of a separate one, per the 4.1 decision below. Functionally complete either way: every README §9.5 capability exists somewhere in the app now.

- [x] 4.1 **Role model decision: reuse `"kitchen"`, no separate `"admin"` role** ✅ done — retroactively closing this out; the decision was actually made back in 3.2 ("no separate `admin` role exists yet; reusing `kitchen` is a deliberate scope decision") and never revisited, since nothing in 4.3/4.4/4.5's actual work gave a reason to. `KitchenNavigator.tsx` is the "new navigator branch" the task asked for — every admin screen (`AddMenuItem`, `TodaysMenu`, `AdminAnalytics`) lives there, not in a separate admin stack.
- [x] 4.2 **Upload/sync UI wired to Phase 3.2's endpoint** ✅ done — retroactively closing this out; this is exactly what `AddMenuItemScreen.tsx` (3.3) already is — a UI submitting to `POST /admin/menu/upload` (3.2's endpoint) — it just hadn't been checked off against this specific task line before.
- [x] 4.3 **Mark items sold out / unavailable in real time** ✅ done (2026-09-13) — README §9.5 / design-spec.md §7.3, completing the sibling half of 3.5's time-window work (that answers "not available *yet*"; this answers "not available *anymore, today*").
  - `PATCH /admin/menu/{item_id}/availability?served_on=YYYY-MM-DD` (`routers/admin_menu.py`), body `{"sold_out": bool}`, kitchen-role gated like every other admin route. `services/menu_store.py::set_item_availability` does a targeted Firestore `update` (raises `NotFound` → 404 for an item that was never actually ingested for that date, rather than silently creating a stub doc).
  - `recommendation_engine.py::_passes_hard_filters` now also excludes `sold_out: true` items — a new, independent check alongside the allergen/dietary/time-window ones, defaulting to available when the key is absent (every item ingested before this feature existed, and the static sample menu, have no `sold_out` key at all).
  - **A real behavioral bug found and fixed while building this:** `menu_store.write_menu_items` used a full `.set()` overwrite, so re-syncing the vendor feed or re-uploading a CSV mid-day would have silently reset a dish staff had just marked sold out back to available, since ingestion (`build_menu_item`) never sets or clears this field itself. Changed to a merge write (`merge=True`) so re-ingestion only touches the fields the source actually supplies, leaving `sold_out` alone. Covered by a dedicated test (`test_re_uploading_preserves_sold_out_status_via_merge`).
  - Frontend: `TodaysMenuScreen.tsx` (`mealsense-app/src/screens/kitchen/`), reachable via a "Today's Menu" button on the Kitchen Dashboard — lists `GET /menu`'s current items with a per-item `Switch`, optimistically updates then reverts on a failed PATCH. Typed against the *actual* backend response shape (snake_case), not the existing `MenuItem` type in `types/index.ts` — see the note below.
  - **Found and fixed as a follow-up (2026-09-13):** `types/index.ts`'s `MenuItem` (and `RecommendationResponse`) interfaces declared camelCase field names (`mealPeriod`, `availableFrom`/`Until`, `proteinG`, `carbsG`, `fatG`, `fiberG`, `sodiumMg`, `sugarG`, `dietaryTags`) that never matched the actual snake_case JSON the backend sends for those fields — confirmed live in `HomeScreen.tsx`, where `item.proteinG`/`carbsG`/`fatG` and `item.availableUntil` were silently `undefined` at runtime (nutrition pills rendering `NaN`, the "Until <time>" station subtitle never showing). Fixed at the source: `MenuItem`/`RecommendationResponse` now declare the real snake_case field names matching `schema.py::build_menu_item`'s canonical shape, and every consumer (`HomeScreen.tsx`, `CartScreen.tsx`) updated to match. `TodaysMenuScreen.tsx`'s local `RawMenuItem` workaround type (written to avoid adding a new instance of this bug) removed in favor of the now-correct shared `MenuItem` type. `npx tsc --noEmit` clean across the whole app; no backend changes needed since the bug was purely in the frontend's type declarations, not the API contract itself.
  - 11 new backend tests (4 in `test_recommendation_engine.py`, 4 in `test_menu_store.py`, 3 in `test_admin_menu.py`), 202/202 passing, 100% coverage on `admin_menu.py` and `menu_store.py`. `npx tsc --noEmit` clean on the whole frontend.
- [x] 4.4 **Aggregate anonymized view** ✅ done (2026-09-14, README §9.5, closes design-spec.md §4.4's `GET /v1/admin/analytics` sketch) — most-recommended items and common dietary constraints on campus, both fully anonymized (no student uid, name, or email in either function's output — only counts).
  - `services/analytics_aggregation.py`: `most_recommended_items` (tallies `recommendation_history` by `menuItemId`, most-common first) and `common_dietary_constraints` (tallies `allergies`/`dietaryIdentity`/`conditions`/`nutritionalFocus` across student — not kitchen — accounts). New `GET /admin/analytics` route, kitchen-role gated like every other admin endpoint.
  - **Deliberately naive for now, per the task's own wording** ("don't do this as a naive per-request scan once order volume is nonzero" — it isn't yet). A live per-request Firestore scan is correct at this pilot's actual data volume; the documented fix if that ever changes is a scheduled nightly rollup, not optimizing this scan in place — and there's no scheduler infrastructure in this project yet (Phase 7) to build that against anyway.
  - **A real gap found and fixed while building this:** `recommendation_history` (Phase 8.0) only stored `menuItemId`, not the item's name — useless for a human-facing "most recommended" list, since history spans many days each with their own `menus/{date}/items` subcollection, so there's no single "today's menu" to resolve an older id's name against. Fixed by denormalizing `menuItemName` onto the record at write time (`write_recommendation`'s new required param) rather than joining at report time — the simpler, correct fix, not a workaround.
  - Frontend: `AdminAnalyticsScreen.tsx`, reachable via an "Analytics" button on the Kitchen Dashboard — bar-chart-style rows for both sections, pull-to-refresh, a visible "anonymized and aggregated only" disclaimer matching the anonymization the backend actually enforces.
  - 9 new backend tests (7 for the aggregation functions directly, 2 route-level) plus every existing `write_recommendation` call site updated for the new required `menu_item_name` param. 233/233 backend tests passing, 100% coverage on both new files, 98% overall. `npx tsc --noEmit` clean, 25/25 Firestore rules tests unaffected (this feature only reads via the backend's own service-account access, same as `scripts/metrics_report.py` — no new client-side Firestore access, no rules changes needed).
- [x] 4.5 **Location-scoped kitchen isolation** ✅ done (2026-09-14, design-spec.md §2.3) — a kitchen account can now only read or act on orders whose `locationId` matches its own, not just any order the moment `role == "kitchen"`.
  - `firestore.rules`: new `locationOf(uid)` + `isKitchenForLocation(locationId)`, replacing bare `isKitchen()` checks on `orders`' `read`/`update` rules. A kitchen account with no `locationId` set matches nothing — fails closed, same posture as the allergen hard filter's own missing-data handling, not "any kitchen account, unscoped" by accident.
  - `orders` `create` now requires a non-empty string `locationId` — `changedKeys().hasOnly([...])` on the existing `update` rule already made it immutable post-creation, no separate change needed there.
  - **A real Firestore `list`-query gotcha, caught before it shipped:** rules can't statically prove every document a `list` query might return satisfies a per-document check unless the query itself constrains that field — so `KitchenDashboardScreen.tsx`'s existing `where('status', 'in', [...])` query would have been rejected outright (not silently filtered) the moment the location-scoped `read` rule went live. Fixed by adding `where('locationId', '==', profile.locationId)` to the query itself, gated on `profile.locationId` actually being loaded first. Added the matching composite index (`locationId`, `status`, `placedAt`) to `firestore.indexes.json` — this project already got burned once by a missing composite index (requirements.md §10, 2026-09-07) and wasn't going to repeat it silently.
  - `CheckoutScreen.tsx` stamps every order with `DEFAULT_LOCATION_ID` (`src/constants/location.ts`, currently `'main'`) — there's no per-location selection UI anywhere in the student app yet, so every order today shares one value. The rules mechanism is real and enforced regardless; only the *value* assigned is a placeholder pending an actual second location.
  - `README-kitchen-accounts.md` updated: manually provisioning a kitchen account now has a required `locationId` step, with an explicit warning that skipping it locks that account out of every order (fails closed, not open).
  - Extended `mealsense-app/tests/firestore.rules.test.js` with cross-location denial cases per the task's own requirement: a kitchen account at one location can't read or advance an order at another, and a kitchen account with no `locationId` set can't read anything. 25/25 rules tests passing (up from 20).
  - No backend (`mealsense-api`) changes — orders are written and read entirely client-side (design-spec.md §4.5), so this was a frontend + rules change only. 224/224 backend tests still passing (unaffected), `npx tsc --noEmit` clean.
  - **Not done as part of this task, deliberately:** no location-selection UI for students, no per-location menu scoping, no second location actually provisioned. This closes the *security* gap ahead of a second location existing, per the task's own framing ("prerequisite," not "build multi-location support now").
  - **Deployed to production 2026-09-14** (`firebase deploy --only firestore:rules,firestore:indexes`, project `mealsense-cb5ab`) — this is live, not just emulator-tested. One manual follow-up remains, outside what this session could do (no production Firestore access): any real kitchen account already provisioned there needs `locationId` set via the Firebase console before it can see any orders again — see `README-kitchen-accounts.md`.

## Phase 5 — Auth: University SSO ✅ closed for v1 (2026-09-15)

Traces to requirements §7.

- [x] 5.1 **Resolved README §12 Open Question #4** ✅ done (2026-09-15) — **email/password is the v1 auth strategy; SSO is explicit future scope, not built now.** Decided with the user directly (not deferred to an unnamed "PM" as the README table had it), even though the pilot school (Santa Clara University) is already known — SCU-specific SSO would mean integrating against SCU IT's actual identity provider (SAML or OAuth via whatever campus SSO system they run, e.g. Okta/Shibboleth-style), which requires a real institutional relationship/registration this project doesn't have, not just an engineering task. Building against a generic or mocked IdP instead would produce integration code with no real target to validate against — false progress, not a working feature. Revisit once there's an actual SCU IT relationship, not before.
- [x] 5.2 **Not built** ✅ correctly out of scope per 5.1 — no OAuth 2.0/SAML integration exists. This is *why* it isn't built, not a gap to come back to on its own; it depends on 5.1's institutional prerequisite, not more engineering time.
- [x] 5.3 **Closed out explicitly** ✅ done (2026-09-15) — README §10's architecture diagram and Auth bullet, and §12 Open Question #4, updated to state this as decided (see README changes). No longer a silent gap: the README now says what's actually true instead of describing a feature that doesn't exist.

## Phase 6 — ML-Enhanced Recommendation (Post-v1)

Traces to requirements §2, design-spec §14. Fully designed (Profile-Adaptive Contextual Bandit, LinUCB), zero implementation. Do not start this before Phase 2 (testing) — the heuristic scorer needs a regression safety net before it gets a second scoring path layered on top of it.

- [ ] 6.1 Profile feature vector encoding (health_goal one-hot, activity_level ordinal, condition/focus binary flags, standardized BMI + age)
- [ ] 6.2 Offline k-means clustering (k=15) job + storage for cluster centroids
- [ ] 6.3 Nearest-centroid cold-start assignment for new students
- [ ] 6.4 Per-cluster LinUCB weight learning from thumbs-up/down feedback
- [ ] 6.5 Fallback to heuristic weights when a cluster has <50 feedback samples
- [ ] 6.6 Shadow-mode logging (compute both heuristic and bandit recommendations, log both, serve only the heuristic one)
- [ ] 6.7 A/B rollout gate: bandit must beat heuristic baseline on thumbs-up rate before full exposure
- [ ] 6.8 Only after 6.1–6.7: measure and report an actual acceptance-rate number — do not put a percentage on this anywhere (resume included) until it's been run

## Phase 7 — Deployment & Infrastructure

Traces to requirements §10.

- [x] 7.1 **Hosting target decided and deployed: Google Cloud Run**, not Vercel ✅ done (2026-09-14) — README's original "Vercel" was written before the Firebase/Firestore decision existed at all; Cloud Run was chosen instead because it keeps the whole backend in the same GCP project (`mealsense-cb5ab`) Firestore already lives in, runs a real long-running ASGI process rather than Vercel's serverless-function wrapper around FastAPI, and pairs naturally with Cloud Scheduler for the nightly vendor-sync job design-spec.md §7.1 always envisioned but was never built.
  - **Live URL:** `https://mealsense-api-983327527722.us-west1.run.app`
  - **Two real, load-bearing bugs found and fixed as a direct result of actually deploying** (not found by any amount of local testing, since both only manifest with no explicit credential file on disk):
    1. `services/firestore_client.py` had no Application Default Credentials path at all — outside the emulator, it unconditionally required `GOOGLE_APPLICATION_CREDENTIALS` to point at a downloaded key file. Cloud Run provides credentials automatically via its metadata server, no key file involved — as originally written, the deployed service would have silently lost all Firestore access, falling back to `sample_menu.py` for every request with no error surfaced. Fixed by adding a `google.auth.default()` fallback branch.
    2. `services/auth.py::_ensure_firebase_admin_initialized` had the identical gap, one layer deeper: it unconditionally required the same key file to verify Firebase ID tokens for every `/admin/*` route. Undetected by 7.1's first bug's fix, since it's a *separate* Firebase Admin SDK credential path from `google.cloud.firestore.Client`. As originally written, every kitchen/admin request against the deployed service would have failed with a **misleading 401 "Invalid or expired token"** — not because any token was actually invalid, but because `firebase_admin.initialize_app()` never succeeded at all, and the resulting `RuntimeError` was silently swallowed by `require_kitchen_role`'s generic `except Exception`. Fixed the same way: fall back to `firebase_admin.initialize_app()` with no explicit credential, which resolves via ADC automatically.
  - **A real test-suite regression found and fixed along the way:** adding the ADC fallback made `google.auth.default()` get attempted locally too — on a machine with no ADC configured, that call times out against GCP's metadata server (~6s), and was being retried on every single "not configured" test call instead of once, taking the suite from ~2s to ~25s. Fixed by caching the negative result at module scope, *deliberately excluded* from `reset_firestore_client()`'s reset (ADC availability is a fact about the machine/network, not per-test state) — cut it to ~12s (the one unavoidable real timeout, paid once per process). Once real local ADC was actually set up (`gcloud auth application-default login`, done as part of this same task), that cost disappears entirely — confirmed, suite back to ~5-9s.
  - **A second, more interesting test regression, also found and fixed:** once real local ADC credentials existed on the dev machine, `test_routes.py`'s entire premise — "Firestore isn't configured in this environment" — silently stopped being true. Two tests that relied on that as ambient fact started either passing coincidentally (real-time-dependent short-circuit) or failing outright. Fixed properly: a new `firestore_not_configured` fixture explicitly mocks `google.auth.default()` to fail, rather than the tests relying on incidental absence of credentials on whatever machine happens to run them — the same principle already applied to `test_firestore_client.py`'s tests (5 new tests added there too, locking in both the ADC success and cached-failure paths with mocks, no reliance on real network calls).
  - **Infrastructure actually provisioned, not just code:** billing enabled on `mealsense-cb5ab` (was not — a real account had to be created); Cloud Run, Cloud Build, Artifact Registry, and Secret Manager APIs enabled; a dedicated least-privilege service account (`mealsense-api-runner@mealsense-cb5ab.iam.gserviceaccount.com`, granted only `roles/datastore.user` and read access to one specific secret — not the broad default compute service account, which this project already had sitting at `roles/editor` from project creation); the Anthropic API key stored in Secret Manager (piped directly from the local `.env`, never printed to any log or terminal output, never baked into the container image — see `.dockerignore`).
  - **Live-verified, not just deployed:** `/health` and `/menu` (exercises the Firestore ADC fix — real query against production Firestore, correctly falls back to the sample menu since no real menu has been uploaded for today) both confirmed working against the actual deployed URL. `/admin/menu/upload` and `/admin/analytics` confirmed correctly returning 401 (not 500) with no auth header, proving the routes are live and reachable — full end-to-end confirmation of the auth fix (a real kitchen login succeeding) needs a real Firebase ID token, which only the actual mobile app can produce; that's the natural next real-world check once 7.2's URL change reaches a device.
  - `Dockerfile` + `.dockerignore` added to `mealsense-api/` — multi-stage not needed at this size, but `.dockerignore` explicitly excludes `.env` (the real secret never enters the build context) and dev-only files.
  - 246 total backend tests, all passing (includes the 5 new in `test_firestore_client.py`, replacing the one removed from `test_auth.py`). `npx tsc --noEmit` clean.
- [x] 7.2 **Environment-based API URL** ✅ done (2026-09-14) — `mealsense-app/src/config/api.ts` now reads `EXPO_PUBLIC_API_BASE_URL` (matching the existing `EXPO_PUBLIC_FIREBASE_API_KEY` convention already used for Firebase config), defaulting to the real deployed Cloud Run URL so a production build needs zero configuration. Local development against a locally-running backend still works by overriding that env var in `.env` — the hardcoded, network-fragile LAN IP is gone.
- [ ] 7.3 Set up dev/staging/prod environment separation
- [x] 7.4 **GitHub Actions CI** ✅ done (2026-09-14) — `.github/workflows/ci.yml`, runs on every push and pull request to `main`. Two jobs, running the full current test suite, not just Phase 2's original scope (which was written before Phases 3-8 added their own test files):
  - `backend-tests`: Python 3.11 + `requirements-dev.txt`, real Firestore emulator (Java via `actions/setup-java`, `firebase-tools` pinned to the exact version `mealsense-app/package.json` already depends on — 15.28.1 — so CI's emulator behaves identically to local dev instead of drifting to whatever "latest" happens to be on a given run), `pytest --cov=services --cov=routers`. 246/246 tests.
  - `frontend`: `npm ci`, `npx tsc --noEmit`, then `npm run test:rules` (the existing Firestore security-rules suite, 25/25) — combined into one job since both already need the same `npm ci`'d `node_modules`.
  - **A real correctness decision, not a default:** the Firestore emulator for `backend-tests` is started as a plain background process (`firebase emulators:start`), not via `firebase emulators:exec` (the wrapper `test:rules` already uses for the Node test). `emulators:exec` injects `FIRESTORE_EMULATOR_HOST` into the *entire* wrapped process's environment — which would silently break every test verifying the "Firestore not configured" fallback (`test_routes.py`'s `firestore_not_configured` fixture, `test_firestore_client.py`'s ADC tests), since `get_firestore_client()` checks `FIRESTORE_EMULATOR_HOST` before ever reaching the mocked ADC path those tests actually exercise. Running the emulator as its own step (waited on via a plain TCP check, then left running unscoped for the rest of the job) mirrors local dev exactly, where a developer runs the emulator in one terminal and `pytest` in another with no shell-wide env var either — each emulator-backed test file sets `FIRESTORE_EMULATOR_HOST` for itself per-test via `monkeypatch` (their `autouse=True` `emulator_env` fixtures), same as it already does locally.
  - **Verified locally before pushing, not just written and hoped:** started the Firestore emulator as its own background process exactly as the workflow does (no `emulators:exec`, no shell-wide `FIRESTORE_EMULATOR_HOST`), confirmed all 246 backend tests still pass under that setup — proving the "not configured" tests genuinely exercise the mocked ADC path rather than coincidentally succeeding against a live emulator. `npm run test:rules` reconfirmed at 25/25 independently.
  - **Deliberately not done:** no coverage-threshold gate, no deploy step, no branch protection rule requiring the check (that's a GitHub repo setting, not something committable) — this task was scoped as "run the test suite on push," not a full release-gating pipeline.

## Phase 8 — Metrics Instrumentation

Traces to requirements §11. All nine README §14 success metrics currently have no way to be measured.

- [x] 8.0 **Thumbs-up/down feedback** ✅ done (2026-09-13) — README §9.2 / §13 describes this as already collected in v1; it wasn't (`/recommendation` was fully stateless, no history, no feedback route, no UI — this closes that gap for real). Traces to design-spec.md §3.3 (`recommendation_history`) and §4.3 (`POST /recommendation/:id/feedback`, sketched but never built).
  - `services/recommendation_history.py` — new Firestore collection, one doc per *served top recommendation* (`write_recommendation`), plus `set_feedback` (changeable, not one-shot — a later vote overwrites) and `get_recommendation` (read path a future recommendation-history view would need too).
  - `POST /recommendation` now writes a history record for the top pick and returns `recommendation_id` in the response — but only when a `uid` is present in the request's profile AND Firestore is actually configured; either miss and the field is simply absent, never a failed request. `/recommendation` itself stays otherwise stateless (menu reads, scoring, hard filters unchanged).
  - New route: `POST /recommendation/{id}/feedback`, body `{"feedback": "thumbs_up"|"thumbs_down"}` — 404 for an unknown id, 422 for an invalid value.
  - **Deliberately scoped to the top pick only, never the alternatives shown via "show more"** — a real design decision, not laziness: the bandit's reward signal (design-spec.md §14.2 Step 4) rewards "the recommended item," singular, and has no causal story for feedback on an item the scoring function didn't choose to serve. Also matches the product's own stated philosophy (README §13: "one confident recommendation is the differentiator," alternatives are a collapsed-by-default escape hatch, not a rating surface).
  - Frontend: thumbs-up/down buttons on `HomeScreen.tsx`'s top `RecommendationCard` only (never rendered on `compact` alternative cards), optimistic update with revert-on-failure, resets when a new recommendation arrives (e.g. after pull-to-refresh).
  - 14 new backend tests (service-level + route-level, both with and without Firestore configured — the latter deliberately deterministic via a mocked `recommend()` rather than depending on real wall-clock meal windows), 216/216 passing, 100% coverage on `routers/recommendations.py` and `services/recommendation_history.py`. `npx tsc --noEmit` clean.
- [x] 8.1 **Pick an analytics approach** ✅ done (2026-09-13) — a dedicated Firestore `analytics_events` collection, not Firebase Analytics. Chosen specifically because it's directly queryable by a Python script using the exact same `services/firestore_client.py` access `mealsense-api` already has (Firebase Analytics data lands in Google Analytics/BigQuery export, a separate pipeline to stand up); also matches every other instrumentation this project already does (`recommendation_history`, `menu_review_queue`) rather than introducing a second system.
- [x] 8.2 **Instrument profile completion, time-to-recommendation, order conversion** ✅ done (2026-09-13) — recommendation relevance itself was already covered by 8.0's thumbs-up/down.
  - **Profile completion** needed no new instrumentation at all — `onboardingComplete` already exists on every `users/{uid}` doc; the report just queries the ratio directly.
  - **Time-to-recommendation**: `src/utils/analytics.ts::logRecommendationFetchTiming`, called from `HomeScreen.tsx` around the `/recommendation` fetch (client-measured elapsed ms), fire-and-forget — a logging failure must never surface to a student who already has their recommendation on screen. New Firestore rule for `analytics_events/{eventId}`: create-your-own only, no client read/update/delete (reporting reads happen server-side via the backend's own Firestore access, same as `menus/{date}/items`). 4 new rules tests (20/20 total passing).
  - **Order conversion**: `recommendationId` threaded through `HomeScreen.tsx` → `CartScreen.tsx` → `CheckoutScreen.tsx` → the placed `Order` doc — but **only from the top recommendation card**, never an alternative (matches 8.0's own scoping: the field is meaningful only for the one item a student was actually pointed at). Used `stripUndefined` (the existing helper from the onboarding-undefined-fields bug) since `recommendationId` is legitimately absent, not null, whenever an alternative or nothing at all was ordered — proactively avoiding the exact bug class this project already hit once.
- [x] 8.3 **Minimal reporting script** ✅ done (2026-09-13) — `mealsense-api/scripts/metrics_report.py` (`python -m scripts.metrics_report`), prints profile completion rate, feedback submission rate, recommendation relevance (thumbs-up rate among feedback given), time-to-recommendation (avg/p95), and order conversion rate against README §14's stated targets. Deliberately a script, not a dashboard, per the task's own wording. Explicitly does *not* attempt the other four README §14 metrics: allergen-safe rate is a structural guarantee proved by `recommendation_engine.py`'s own tests, not something live data would reveal; WAU and order-status-accuracy were never in Phase 8's agreed scope; the ML thumbs-up-vs-baseline metric has no meaning until Phase 6 exists.
  - 8 new backend tests (`test_metrics_report.py` — the four metric functions individually, `_pct`/`_percentile` directly, plus a full `main()` smoke test), separate from the 4 new Firestore rules tests noted under 8.2. 224/224 backend tests passing overall, 98% coverage (`scripts/metrics_report.py` itself at 94% — the two uncovered branches are alternate "no data yet" print formatting, not logic gaps). `npx tsc --noEmit` clean, 20/20 Firestore rules tests passing.
- [x] 8.4 **Recommendation history view** ✅ done (2026-09-14, README §9.4: "view recommendation history (last 7 days)") — closes the one piece 8.0 deliberately left open (`services/recommendation_history.py::get_recommendation`'s docstring called this out directly as the future read path).
  - `services/recommendation_history.py::get_recent_recommendations(student_id, days=7, limit=50)` — a single equality filter (`studentId`) with the 7-day cutoff and descending sort both done in Python, not a second Firestore condition, so this needs no new composite index. String comparison on `recommendedAt` works correctly (not just happens to) because every value is written via the same `datetime.now(timezone.utc).isoformat()` format, and ISO 8601 UTC timestamps sort lexicographically in chronological order. Same "naive scan is fine at this pilot's volume" stance as 4.4's aggregation queries.
  - New route: `GET /recommendation/history?student_id=<uid>&days=7`. No auth token verification — matches this route family's existing trust model (`POST /recommendation` already trusts a client-supplied `profile.uid` with no server-side identity check); this doesn't introduce a new gap, just reads via the same trust boundary that already exists.
  - Frontend: `RecommendationHistoryScreen.tsx`, reachable via a new "Recommendation History" row on `ProfileScreen.tsx` — matches README §9.4's own placement of this feature under Profile, not Order History (a different, already-existing screen). Shows item name, meal period, score, timestamp, and the thumbs-up/down feedback badge if one was given.
  - **A real flaky-test bug found and fixed while adding these tests:** three existing `test_analytics_aggregation.py` tests asserted a specific item appeared within `most_recommended_items(db, limit=50)`'s results — safe when that test file was small, but `recommendation_history` is a shared collection across the whole suite, and this session's own test additions grew it enough that a single-count item could fall outside an arbitrary top-50 depending on Firestore's query iteration order for tied counts. Fixed by raising those three tests' limit to a value large enough to be effectively unbounded at this suite's real data volume, rather than the production code's default limit being the issue (`most_recommended_items`'s default `limit=10` is correct product behavior for an admin-facing list; the bug was test-only).
  - 7 new backend tests (`test_recommendation_history.py` — 5 for `get_recent_recommendations`, 2 route-level). 240/240 backend tests passing (up from 233 before this task), 100% coverage on `recommendation_history.py` and `recommendations.py`. `npx tsc --noEmit` clean, confirmed stable across repeated runs (not a one-time pass) given the shared-state flakiness just found and fixed.
